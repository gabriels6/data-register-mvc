package com.gomes.dataregister.finance.repository;

import com.gomes.dataregister.finance.model.AccountingType;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AccountingTypeRepository extends JpaRepository<AccountingType, Integer> {
}
